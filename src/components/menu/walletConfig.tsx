import { LedgerIcon, BitteIcon } from "./icons2";

export const walletIconConfig: Record<string, any> = {
  ledger: <LedgerIcon className="" style={{ transform: "scale(0.2)" }} />,
  "bitte-wallet": <BitteIcon />,
};
